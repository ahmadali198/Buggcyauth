services:
  - type: web
    name: google-express-oauth
    env: node
    buildCommand: "npm run render-build"
    startCommand: "npm start"
    plan: free
    envVars:
      - key: NODE_VERSION
        value: 18
